// Get all elements with the text-elements class
document.querySelectorAll(".text-elements")
// Remove the text-elements class from each element
  .forEach(element => {
  element.classList.remove("text-elements");
});
